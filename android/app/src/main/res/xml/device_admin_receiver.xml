<device-admin xmlns:android="http://schemas.android.com/apk/res/android">
    <uses-policies>
        <policy android:name="android.app.device_admin" />
        <policy android:name="android.app.lock_task" />
        <policy android:name="limit_password" />
          <policy android:name="android.app.deviceadmin.ACTIVATE_DEVICE_ADMIN" />
    </uses-policies>
</device-admin>